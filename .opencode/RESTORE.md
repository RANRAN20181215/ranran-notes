# 一键恢复步骤

## 项目配置（已入 Git，Gitee 自动备份）
```bash
git checkout main
git pull
```
`opencode.json` 和 `.opencode/memory.md` 会自动恢复。

## 全局配置
将 `.opencode/global-config.backup.json` 复制到 `~\.config\opencode\opencode.json`
