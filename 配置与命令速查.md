---
tags: [opencode, 配置, 速查]
---

# 配置与命令速查

## 已安装的 Skills

| 技能 | 名称 | 用法 |
|------|------|------|
| **张雪峰** | `zhangxuefeng-perspective` | 高考志愿/考研/职业规划分析 |
| **温儒敏** | `wenrumin-perspective` | 语文教育/阅读/教材分析 |

### 使用方式
在对话中直接说：
- "用张雪峰的视角分析..."
- "温儒敏怎么看..."
- "切换到张雪峰"

退出角色：说「退出」或「切回正常」

---

## 已配置的 MCP 服务器

| MCP | 类型 | 用法 |
|-----|------|------|
| **Context7** | Remote | 搜索技术文档 → 加 `use context7` |
| **Grep by Vercel** | Remote | 搜索 GitHub 代码示例 → 加 `use gh_grep` |
| **FlowUs 息流** | Remote | 读写 FlowUs 知识库 → 加 `use flowus` |

### MCP 管理命令
```bash
# 查看所有 MCP 状态
opencode mcp list

# 认证 MCP（需要 OAuth 的）
opencode mcp auth <server-name>

# 调试 MCP 连接
opencode mcp debug <server-name>

# 登出 MCP
opencode mcp logout <server-name>
```

### 配置位置
`C:\Users\ASUS\.config\opencode\opencode.json`

---

## Obsidian ↔ FlowUs 同步

### 同步脚本
```powershell
# 运行同步（在 OB 仓库目录下）
cd D:\OB\ranran
.\sync-obsidian-to-flowus.ps1
```

### 脚本位置
`D:\OB\ranran\sync-obsidian-to-flowus.ps1`

### 注意事项
- 脚本会跳过 `.agents/` `.obsidian/` `.claude/` `.smart-env/` `copilot/` 等目录
- 每次运行会**创建新页面**，不会覆盖已有页面
- 如需清理重复页面，运行清理脚本

---

## OpenCode 常用命令

```bash
# 查看当前配置
opencode mcp list

# 列出已安装 skills
npx skills ls
npx skills ls --json

# 安装新 skill
npx skills add <github-repo>

# 全局安装 skill
npx skills add <github-repo> -g
```

---

## 环境信息

| 项目 | 内容 |
|------|------|
| OB 仓库路径 | `D:\OB\ranran` |
| OpenCode 全局配置 | `C:\Users\ASUS\.config\opencode\opencode.json` |
| Skills 存储路径 | `D:\OB\ranran\.agents\skills\` |
| Skills 锁文件 | `D:\OB\ranran\skills-lock.json` |
| FlowUs Token | 已配置 |
