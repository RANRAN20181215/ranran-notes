---
tags:
---

# 配置与命令速查

## 已安装的 Skills

| 技能 | 名称 | 用法 |
|------|------|------|
| **张雪峰** | `zhangxuefeng-perspective` | 高考志愿/考研/职业规划分析 |
| **温儒敏** | `wenrumin-perspective` | 语文教育/阅读/教材分析 |

### 使用方式
在对话中直接说：
- "用张雪峰的视角分析..."
- "温儒敏怎么看..."
- "切换到张雪峰"

退出角色：说「退出」或「切回正常」

---

## 已配置的 MCP 服务器

| MCP | 类型 | 用法 |
|-----|------|------|
| **Context7** | Remote | 搜索技术文档 → 加 `use context7` |
| **Grep by Vercel** | Remote | 搜索 GitHub 代码示例 → 加 `use gh_grep` |
| **FlowUs 息流** | Remote | 读写 FlowUs 知识库 → 加 `use flowus` |

### MCP 管理命令
```bash
# 查看所有 MCP 状态
opencode mcp list

# 认证 MCP（需要 OAuth 的）
opencode mcp auth <server-name>

# 调试 MCP 连接
opencode mcp debug <server-name>

# 登出 MCP
opencode mcp logout <server-name>
```

### 配置位置
`C:\Users\ASUS\.config\opencode\opencode.json`

---

## Obsidian MCP

OpenCode 可以直接读写 Obsidian 笔记（通过 `obsidian-mcp`）：

```bash
# 在 opencode.json 中已配置，自动连接 vault
# 对话中 MCP 工具自动可用：read-note / create-note / edit-note / search-vault 等
```

### 工作原理
- `npx obsidian-mcp D:\OB\ranran` 直接读取文件系统
- 无需 Obsidian 插件，无需启动 Obsidian
- 配置位置：`C:\Users\ASUS\.config\opencode\opencode.json` → `mcp.obsidian`

---

## Git 版本控制

### MinGit 安装路径
`C:\tools\git-minimal\cmd\git.exe`（已添加到用户 PATH）

### 远程仓库
```
https://github.com/RANRAN20181215/ranran-notes.git
```

### 常用命令
```bash
git status                # 查看变更
git add -A                # 暂存所有
git commit -m "信息"      # 提交
git push                  # 推送到 GitHub
```

---

## Quartz 静态网站 & GitHub Pages 部署

### 本地构建
```bash
cd D:\OB\ranran
npx quartz build --directory . --output public
```

### 本地预览
```bash
npx quartz build --directory . --output public --serve
# 访问 http://localhost:8080
```

### 自动部署
- 推送 `main` 分支后，GitHub Actions 自动部署
- 网站地址：`https://RANRAN20181215.github.io/ranran-notes/`
- 工作流文件：`.github/workflows/deploy-pages.yml`

### 配置修改
- `quartz.config.ts` — 标题、主题、插件、忽略规则
- `quartz.layout.ts` — 页面布局（导航、搜索等）
- 修改后推送到 GitHub 自动重新部署

---

## Obsidian ↔ FlowUs 同步

### 同步脚本
```powershell
cd D:\OB\ranran
.\sync-obsidian-to-flowus.ps1
```

### 注意事项
- 跳过 `.agents/` `.obsidian/` `.smart-env/` `copilot/` 等目录
- 每次运行**创建新页面**，不会覆盖已有页面

---

## OpenCode 常用命令

```bash
opencode mcp list          # 查看 MCP 状态
npx skills ls              # 列出 skills
npx skills add <repo>      # 安装 skill
```

---

## 环境信息

| 项目 | 内容 |
|------|------|
| OB 仓库路径 | `D:\OB\ranran` |
| OpenCode 全局配置 | `C:\Users\ASUS\.config\opencode\opencode.json` |
| Skills 存储路径 | `D:\OB\ranran\.agents\skills\` |
| Skills 锁文件 | `D:\OB\ranran\skills-lock.json` |
| FlowUs Token | 已配置 |
| Git 路径 | `C:\tools\git-minimal\cmd\git.exe` |
| GitHub 仓库 | `github.com/RANRAN20181215/ranran-notes.git` |
| 部署方式 | GitHub Pages + Actions |
| 网站地址 | `https://RANRAN20181215.github.io/ranran-notes/` |
